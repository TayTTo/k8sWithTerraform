# group_vars/bastion.yml

ansible_user: ubuntu
ansible_ssh_private_key_file: ~/.ssh/aws
gather_facts: false
ansible_ssh_common_args: >
  -o ProxyCommand="ssh -i {{ ansible_ssh_private_key_file }} -W %h:%p -q {{ ansible_user }}@{{ groups['tag_Role_kibana'][0] }}"
